version: "1"

# gridfire_template_version: stage3-go-cli/v1

github:
  owner: BrettHamlin
  repo: gum

reviewer_dispatch:
  strategy: progressive
  clustering:
    mode: lsp_preferred
    regex_fallback: true

reviewers:
  - name: command-contracts
    file: .harness/agents/command-contracts.md
    description: Checks CLI command contracts, flags, arguments, exit codes, and stdout/stderr behavior.
    path_hints: ["main.go", "**/command.go", "**/commands/**", "**/options.go", "cmd/**"]
    language_hints: [go]
    mandatory_triggers: ["command", "flag", "argument", "stdout", "stderr", "exit code", "help"]
    passing_grade: C
    max_retries: 5

  - name: terminal-ux
    file: .harness/agents/terminal-ux.md
    description: Checks terminal UX, TTY/non-TTY behavior, scriptability, output formatting, and prompt behavior.
    path_hints: ["main.go", "**/command.go", "**/options.go", "**/tty/**", "**/terminal/**", "examples/**"]
    language_hints: [go, shell]
    mandatory_triggers: ["tty", "terminal", "interactive", "prompt", "color", "format", "script"]
    passing_grade: C
    max_retries: 5

codegen:
  cli_provider: codex-cli
  default_model: gpt-5.5

stages:
  planner:
    explore_model: gpt-5.5
  code_gen_feature:
    model: gpt-5.5
    timeout_s: 21600
  code_gen_test:
    model: gpt-5.5
  code_gen_e2e_test:
    model: gpt-5.5
    timeout_s: 21600

ci:
  provider: github-actions
  build:
    workflow: ".github/workflows/gridfire-build.yml"
    timeout_sec: 1800
  test:
    workflow: ".github/workflows/gridfire-test.yml"
    timeout_sec: 1800
  failure_extraction:
    - source: annotations
    - source: junit-xml
      path: "test-results/*.xml"
    - source: ai-extract

gates:
  plan:
    auto_approve: true
  ac:
    auto_approve: true
  merge_review:
    auto_approve: true

inner_loop:
  reviewer_per_max: 2
  reviewer_total_max: 5
  build_max: 3
  test_max: 3
  build_test_total_max: 5
  budget_reset_on_user_changes: true
